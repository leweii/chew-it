{
  "name": "chew-it",
  "version": "0.1.5",
  "description": "Obsidian plugin to analyze the current note with an LLM (Claude / OpenAI-compatible).",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "keywords": ["obsidian", "plugin", "llm", "claude", "anthropic"],
  "author": "Jakob",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.12.0",
    "esbuild": "^0.21.5",
    "obsidian": "latest",
    "typescript": "^5.4.5"
  }
}
